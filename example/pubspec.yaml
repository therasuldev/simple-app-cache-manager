name: simple_app_cache_manager_example
description: Flutter Package for Managing Application Cache
publish_to: 'https://github.com/therasuldev/simple_app_cache_manager'

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  simple_app_cache_manager:
    path: ../

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true